# python-os-helpers

Frozen-app directory helpers, path utilities, font discovery, and misc OS helpers. KotOR `Installation` type checks removed.

## Install

```bash
pip install -e .
# or from GitHub:
pip install git+https://github.com/bodencrouch/python-os-helpers
```

## Origin

Extracted from the [PyKotor](https://github.com/bodencrouch/PyKotor) monorepo `utility` / related packages.
KotOR-specific couplings were removed or made optional for standalone use.

### DAG
Optional peer: `loggerplus`. Used by `github-app-updater`.

## License

LGPL-3.0-or-later
